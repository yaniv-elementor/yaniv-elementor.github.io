import React from 'react';
import { AppBarProps } from '@elementor/ui/AppBar';
import { ToolbarProps } from '@elementor/ui/Toolbar';
import { SvgIconProps } from '@elementor/ui/SvgIcon';
import './DialogHeader.types';
export interface DialogHeaderProps extends AppBarProps {
    children?: React.ReactNode;
    className?: string;
    logo?: React.ReactNode | false;
    onClose?: (event: React.SyntheticEvent) => void;
    slotProps?: {
        logo?: SvgIconProps;
        toolbar?: ToolbarProps;
    };
}
declare const slots: {
    root: {
        slot: "root";
        name: "MuiDialogHeader";
    };
    logo: {
        slot: "logo";
        name: "MuiDialogHeader";
    };
    toolbar: {
        slot: "toolbar";
        name: "MuiDialogHeader";
    };
};
export type DialogHeaderSlots = keyof typeof slots;
declare const DialogHeader: React.FC<DialogHeaderProps>;
export default DialogHeader;

import React from 'react';
import { IconButtonProps } from '@elementor/ui/IconButton';
import { SvgIconProps } from '@elementor/ui/SvgIcon';
import './CloseButton.types';
export interface CloseButtonProps extends Omit<IconButtonProps, 'size' | 'color'> {
    color?: 'default' | 'inherit';
    slotProps?: {
        icon?: SvgIconProps;
    };
}
declare const slots: {
    root: {
        slot: "root";
        name: "MuiCloseButton";
    };
    icon: {
        slot: "icon";
        name: "MuiCloseButton";
    };
};
export type CloseButtonSlots = keyof typeof slots;
declare const CloseButton: React.FC<CloseButtonProps>;
export default CloseButton;
